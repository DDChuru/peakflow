# Prevent accidentally committing sensitive files
.env* filter=git-crypt diff=git-crypt
*.key filter=git-crypt diff=git-crypt
credentials.md filter=git-crypt diff=git-crypt

# Mark as binary to prevent merging
.env* binary
*.key binary
